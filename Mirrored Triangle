#input the number of rows 
rows = int(input("Enter the number of rows :"))

#loop to print the mirored right-angle triangle
for i in range(1 , rows+1):
    spaces = rows - i
    stars = i
    print(" " * spaces + "*" * stars) 
